name: build-desktop-packages

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            script: powershell -ExecutionPolicy Bypass -File scripts/build_windows.ps1
            artifact: dist/ETFPlanAssistant.exe
          - os: macos-latest
            script: bash scripts/build_macos.sh
            artifact: dist/ETFPlanAssistant-Core.dmg
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: ${{ matrix.script }}
      - uses: actions/upload-artifact@v4
        with:
          name: desktop-package-${{ runner.os }}
          if-no-files-found: error
          path: ${{ matrix.artifact }}
